<!DOCTYPE html>
<html>
<head>
    <title>Profile</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="profile-container">
        <h1>Your Profile</h1>

        <div class="profile-tabs">
            <button class="tab-button active" data-tab="info">Важная информация</button>
            <button class="tab-button" data-tab="bio">О себе</button>
            <button class="tab-button" data-tab="additional">Дополнительно</button>
        </div>

        <div id="info" class="tab-content active">
            <img src="<%= user.profilePicture || '/images/default-profile.png' %>" alt="Profile Picture" width="150">
            <h2>Update Picture</h2>
            <form action="/profile/upload" method="POST" enctype="multipart/form-data">
                <input type="file" name="profilePicture">
                <button type="submit">Upload</button>
            </form>
            <form action="/profile/remove-picture" method="POST">
                <button type="submit">Remove Profile Picture</button>
            </form>

            <p>Username: <%= user.username %></p>
            <p>Email: <%= user.email %></p>
        </div>

        <div id="bio" class="tab-content">
            <p>Bio: <%= user.bio %></p>
            <form action="/profile/edit" method="POST">
                <div>
                    <label for="bio">Update Bio:</label>
                    <textarea id="bio" name="bio"><%= user.bio %></textarea>
                </div>
                <button type="submit">Update Bio</button>
            </form>
        </div>

        <div id="additional" class="tab-content">
            <p>First Name: <%= user.firstName %></p>
            <p>Last Name: <%= user.lastName %></p>
            <p>Location: <%= user.location %></p>
            <p>Website: <%= user.website %></p>
        </div>

        <a href="/profile?edit=true">Edit Profile</a>
        <a href="/auth/logout">Logout</a>

        <h2>Danger Zone</h2>
        <form action="/profile/delete" method="POST">
            <button type="submit">Delete Account</button>
        </form>
    </div>

    <script src="/script.js"></script>
</body>
</html>