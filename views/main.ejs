<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            padding-top: 56px;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <a class="navbar-brand" href="/">Web App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/profile">Profile</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/auth/logout">Logout</a>
            </li>
        </ul>
    </div>
</nav>

<main class="container">
    <div class="jumbotron">
        <h1 class="display-4">Welcome, <%= user.username %>!</h1>
        <p class="lead">Here's a quick overview of your account.</p>
        <hr class="my-4">

        <div class="row">
            <div class="col-md-4">
                <h3>Profile Information</h3>
                <img src="<%= user.profilePicture || '/images/default-profile.png' %>" alt="Profile Picture" class="img-thumbnail rounded-circle" width="100">
                <p>Email: <%= user.email %></p>
                <a href="/profile" class="btn btn-secondary">Edit Profile</a>
            </div>

            <div class="col-md-8">
                <h3>Recent Notes</h3>
                <% if (notes && notes.length > 0) { %>
                    <ul class="list-group">
                        <% notes.forEach(note => { %>
                            <li class="list-group-item"><%= note.title %></li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <p>No notes yet. <a href="/profile">Add some in your profile!</a></p>
                <% } %>
            </div>
        </div>
    </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>